name: Platform Docs

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  plan:
    name: Plan platform docs shards
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GRADLE_OPTS: -Dorg.gradle.daemon=false -Dorg.gradle.vfs.watch=false -Xmx4g
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Write shard matrix
        run: ./gradlew -q writePlatformDocsShardMatrix

      - name: Publish shard matrix
        id: matrix
        shell: bash
        run: |
          {
            echo 'matrix<<EOF'
            cat build/platform-docs-shard-matrix.json
            echo EOF
          } >> "$GITHUB_OUTPUT"

  build-guides:
    name: Build guide docs shard ${{ matrix.shard }}
    runs-on: ubuntu-latest
    timeout-minutes: 360
    needs:
      - plan
      - test-layout
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.matrix) }}
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GRADLE_OPTS: -Dorg.gradle.daemon=false -Dorg.gradle.vfs.watch=false -Xmx4g
      PLATFORM_DOCS_GUIDE_SHARD_COUNT: ${{ matrix.shard_count }}
      PLATFORM_DOCS_GUIDE_SHARD_INDEX: ${{ matrix.shard }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Build guide docs shard
        run: ./gradlew -q verifyPlatformAlignment stagePlatformGuideDocsArtifact

      - name: Upload guide docs shard
        uses: actions/upload-artifact@v6
        with:
          name: guide-docs-shard-${{ matrix.shard }}
          path: build/guide-docs-artifact
          if-no-files-found: error
          retention-days: 1

  test-layout:
    name: Test two-doc layout
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: plan
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GRADLE_OPTS: -Dorg.gradle.daemon=false -Dorg.gradle.vfs.watch=false -Xmx4g
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Test two-doc layout
        run: |
          ./gradlew -q -PplatformDocs.projectSlugs=core,serde verifyPlatformDocs
          ./gradlew :buildSrc:test

  render:
    name: Render platform docs
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: build-guides
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GRADLE_OPTS: -Dorg.gradle.daemon=false -Dorg.gradle.vfs.watch=false -Xmx4g
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Download guide docs shards
        uses: actions/download-artifact@v6
        with:
          pattern: guide-docs-shard-*
          path: .
          merge-multiple: true

      - name: Render and verify platform docs
        run: ./gradlew -q verifyRenderedPlatformDocs

      - name: Upload generated site
        uses: actions/upload-artifact@v6
        with:
          name: micronaut-platform-docs-site
          path: build/site
          if-no-files-found: error
          retention-days: 14

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build/site

  deploy:
    name: Deploy GitHub Pages
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: render
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
